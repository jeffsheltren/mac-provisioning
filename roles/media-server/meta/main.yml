---
dependencies:
  - { role: geerlingguy.homebrew }
  # - { role: geerlingguy.mas, when: ansible_connection == "local"  } # For this, we need to run it locally, since over SSH it seems it just hangs (https://github.com/mas-cli/mas/issues/71)
